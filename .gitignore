# Gradle
.gradle/
build/
!**/src/**/build/
**/build/

# Eclipse IDE output (compiled classes + resources placed under bin/)
**/bin/

# Java
*.class
*.jar
*.war
*.ear
*.zip
*.tar.gz
*.rar

# Maven
target/
**/target/

# IDEs
.idea/
*.iml
*.iws
*.ipr
.vscode/
.settings/
.classpath
.project
.factorypath

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Node/Expo (Handled in mobile/ but good to have here too)
node_modules/
.expo/
dist/
web-build/

# Docker Local Data
*_data/
pgdata/
neo4jdata/

# Environment files
.env
.env.local
.env.*.local

# Locust outputs
tests/performance/reports/
tests/performance/locust-report-*.html
tests/performance/locust-*.csv

# Pipeline security scanner outputs (transient; permanent evidence lives in docs/report/security/)
tests/security/reports/

# Runtime file-upload storage (form-service `form.storage.root` and file-service
# `file.storage.root`, both default to `uploads/`). Created at runtime/test; never versioned.
uploads/
**/uploads/

# Generated kubeconfig for the Jenkins container (created by setup-kubeconfig.ps1)
jenkins/config/.kubeconfig-incontainer

# Real Docker Hub registry credentials for Kaniko (never commit).
# Only the .example template is versioned; the .yaml is built from it locally.
k8s/jenkins/dockerhub-secret.yaml
k8s/circleguard-secrets.yaml
jenkins/config/.dockerconfig.json

# Jenkins AKS credentials Secret (only the .example is versioned)
k8s/jenkins/jenkins-secret.yaml

CLAUDE.md
PLAN_IMPLEMENTACION_V2.md
GUIA_DECISIONES_ARQUITECTONICAS.md
Handoff.md
ONBOARDING.md
RUNBOOK_PIPELINES.md

# Terraform
**/.terraform/
**/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
*.plan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# tfvars con valores reales NO se versionan
infra/terraform/environments/*/terraform.tfvars.local
infra/terraform/bootstrap/terraform.tfvars.local