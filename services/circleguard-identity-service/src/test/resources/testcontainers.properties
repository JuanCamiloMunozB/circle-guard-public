# Force Testcontainers to use the Docker Unix socket explicitly.
# Docker Desktop exposes this socket inside WSL2 distros when
# "WSL Integration" is enabled for the distro in Docker Desktop settings.
# Without this, Testcontainers may fall back to other strategies and fail.
docker.client.strategy=org.testcontainers.dockerclient.UnixSocketClientProviderStrategy
